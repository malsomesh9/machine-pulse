# Models

Versioned model artifacts will be generated only from documented baseline data.
Large or local artifacts are ignored by default; reproducible training metadata
and evaluation summaries should accompany any published model.
